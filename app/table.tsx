import React from 'react';
import PaceTable from '../src/components/PaceTable';

/** Aba Tabela. Rota "/table". A PaceTable já tem a própria rolagem. */
export default function TableScreen(): React.ReactElement {
  return <PaceTable />;
}
